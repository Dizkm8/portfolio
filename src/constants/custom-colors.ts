const black = '#000000';

const gray = '#333333';

const lightGray = '#808080';

const white = '#ffffff';

const customColors = {
    black,
    white,
    gray,
    lightGray,
};

export default customColors;
